import React from 'react'

const TodoFilter = () => (
    <div>
        Show {' '}
        <button>All</button>{' '}
        <button>Active</button>{' '} 
        <button>Completed</button>
    </div>
)

export default TodoFilter