import React from 'react'

const TodoAdd = (prop) => {
    return (
        <div>
            <form>
                <input />
                <button>Add Too</button>
            </form>
        </div>
    )
}

export default TodoAdd